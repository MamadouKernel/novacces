## .NET
bin/
obj/
*.user
*.suo
.vs/
*.dll
*.pdb

## Secrets & clés cryptographiques — NE JAMAIS committer
## (les clés ES256 vivent dans dotnet user-secrets ou un secret manager)
*.pem
*.key
*.pfx
*.p12
appsettings.*.Local.json
appsettings.Development.json
secrets.json

## Documents commerciaux bruts (hors dépôt — équivalents texte dans docs/*.md)
/*.docx
/*.pdf
/*.zip

## Front-end (Tailwind)
node_modules/

# Instructions locales de développement — ne jamais publier
CLAUDE.md
**/CLAUDE.md

# Sorties de build hors bin/obj (artefacts de vérification locale)
artifacts/

# Configuration locale de l'outillage Claude Code — jamais publier
.claude/
