<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <meta name="theme-color" content="#0e2a3a" />
    <meta name="lang" content="fr" />
    @* Typographies de la maquette — repli système automatique si hors-ligne. *@
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link rel="stylesheet"
          href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@500;600;700&family=Public+Sans:wght@400;500;600;700&family=IBM+Plex+Mono:wght@400;500&display=swap" />
    @* Empreinte du fichier en query string : sans elle, un navigateur qui a
       mis en cache l'ancien app.css continue de l'appliquer après déploiement.
       Le symptôme est trompeur — le HTML est à jour, les styles non — et fait
       conclure à tort que « le rendu ne colle pas ». *@
    <link rel="stylesheet" href="app.css?v=@AssetVersion.Css" />
    <link rel="stylesheet" href="NovAcces.Web.styles.css" />
    <link rel="icon" type="image/svg+xml" href="favicon.svg" />
    <link rel="icon" type="image/png" href="favicon.png" />
    <HeadOutlet @rendermode="new InteractiveServerRenderMode(prerender: false)" />
</head>

<body>
    <Routes @rendermode="new InteractiveServerRenderMode(prerender: false)" />
    <script src="_framework/blazor.web.js"></script>
    <script src="js/app.js"></script>
</body>

</html>
